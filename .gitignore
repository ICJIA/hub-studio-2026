# Dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Nuxt build & dev artifacts
.nuxt
.nuxtrc
.output
.nitro
.cache
.data
dist

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Test / coverage output
coverage
.vitest
test-results
playwright-report

# Editor / OS cruft
.vscode/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln

# Misc local files
*.local

# Build-time copies of the manager-facing spec (scripts/copy-spec.mjs) — docs/ is the source
public/spec/

# Claude Code session worktrees (isolated checkouts; never sources)
.claude/worktrees/
